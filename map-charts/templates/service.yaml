apiVersion: v1
kind: Service
metadata:
  name: {{ include "map-charts.fullname" . }}
  labels:
    {{- include "map-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "map-charts.selectorLabels" . | nindent 4 }}
