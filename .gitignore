# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
out/

# Environment files
.env
.env.local
.env.production

# IDE
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# ========================================
# LARGE DATA FILES
# ========================================
# nodes.bin, graph.weights, graph.targets are tracked via Git LFS (see .gitattributes)
data/nodes.txt
data/edges.txt

# Source data (too large for GitHub, even with LFS bandwidth in mind)
*.osm.pbf
map_central_zone.csv

# C++ build artifacts
cpp-engine/*.exe
cpp-engine/*.o
cpp-engine/src/*.exe
cpp-engine/tools/*.exe

# Keep these in git (smaller files, no LFS needed)
# data/places.bin (0.5 MB) - OK
# data/graph.offset (64 MB) - OK
