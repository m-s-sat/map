[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["backend/**", "cpp-engine/**", "Dockerfile", "railway.toml"]

[deploy]
healthcheckPath = "/api/nodes/stats"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
